{
  "name": "@automorph/verification-agent",
  "version": "1.0.0",
  "description": "Standalone claim verification agent — mark, shortlist, and cite LLM response claims",
  "main": "core.ts",
  "types": "core.ts",
  "scripts": {
    "start":   "tsx server.ts",
    "dev":     "tsx --watch server.ts",
    "build":   "tsc --outDir dist",
    "test":    "echo 'no tests yet'"
  },
  "keywords": ["claim-verification", "citations", "LLM", "fact-checking", "agent"],
  "license": "MIT",
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.18.2",
    "cors":    "^2.8.5"
  },
  "devDependencies": {
    "tsx":         "^4.0.0",
    "typescript":  "^5.0.0",
    "@types/express": "^4.17.21",
    "@types/cors":    "^2.8.17",
    "@types/node":    "^20.0.0"
  }
}
